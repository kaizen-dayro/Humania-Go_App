## Resumen

<!-- Qué cambia y por qué, en 1-3 líneas. -->

## Checklist antes de aprobar

- [ ] `npm run build` y `npm run lint` pasan en local (el CI de este PR también lo verifica automáticamente).
- [ ] Probado en local / en el Preview Deployment de este PR antes de pedir aprobación.
- [ ] **¿Este PR incluye una migración nueva en `supabase/`?** Si es sí: la migración fue **ejecutada manualmente en el panel de Supabase de producción y verificada** antes de mergear este PR — nunca la corre CI ni ningún proceso automático.
- [ ] Si hay un texto nuevo o cambiado de cara al candidato/usuario, fue aprobado explícitamente antes de este PR.
- [ ] Si aplica, el Documento 14 (y el documento de fase correspondiente) quedaron actualizados.

## Migración de Supabase (si aplica)

<!-- Nombre del archivo de migración, o "N/A" si este PR no toca el esquema. -->
